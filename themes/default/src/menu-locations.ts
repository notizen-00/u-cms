import { defineMenuLocation } from "@unej-cms/sdk-theme";

export const primaryMenuLocation = defineMenuLocation({
  id: "primary",
  label: "Navigasi Utama",
  description: "Menu yang ditampilkan di header (region \"header\").",
});
